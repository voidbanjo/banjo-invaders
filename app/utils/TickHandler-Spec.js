import { expect } from 'chai';

describe('utils/TickHandler', () => {
  describe('constructor', () => {
    // doesnt really do nuthin right now
  });

  describe('tick', () => {
    // doesnt really do nything right now
  });
});
